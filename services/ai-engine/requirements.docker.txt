# FastAPI and web framework
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# AI API clients (lightweight, no heavy ML models; no OpenAI per values rule)
anthropic>=0.8.1
google-generativeai>=0.3.2

# Vector database
qdrant-client>=1.7.1

# Database
asyncpg>=0.29.0
redis>=4.5.2,<5.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.28.1
requests>=2.31.0

# Monitoring
prometheus-client>=0.19.0

# Lightweight NLP (instead of heavy torch/transformers)
nltk>=3.8.1
numpy>=1.24.3

# Note: Heavy ML packages (torch, transformers, sentence-transformers)
# are excluded to reduce image size and build time.
# Use external embedding services or mount a pre-trained model volume.
