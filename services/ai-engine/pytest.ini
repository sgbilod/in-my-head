[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = strict
addopts = --strict-markers --tb=short
markers =
    unit: Unit tests (no external deps)
    integration: Tests requiring DB/Redis/services
    ml: Tests requiring ML models (sentence-transformers, torch)
