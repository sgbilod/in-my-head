# Ultra-Minimal Requirements - Just to Start Services
# Focuses on packages with pre-built wheels for Python 3.13

# Web framework (essential)
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# Database (try without psycopg2 for now)
# psycopg2-binary==2.9.9  # May not have Python 3.13 wheels yet
sqlalchemy>=2.0.23

# Redis (compatible with celery)
redis>=4.5.2,<8.0.0

# Minimal utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Qdrant client (Python 3.13 compatible)
qdrant-client>=1.7.1

# OpenAI (for embeddings)
openai>=1.3.7
